<UserControl x:Class="FailureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:global="clr-namespace:"
        mc:Ignorable="d"
        d:DesignHeight="300" d:DesignWidth="300" >
    <UserControl.DataContext>
        <global:FailureViewModel/>
    </UserControl.DataContext>
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Style="{StaticResource MaterialDesignSubheadingTextBlock}" Text="{Binding FailureDescription}" TextWrapping="Wrap"/>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button x:Name="Exit" Content="Exit"/>
            <Button x:Name="ReportError"  Content="Report Error" Margin="10,0"/>
        </StackPanel>
        <ScrollViewer>
            <TextBlock DockPanel.Dock="Top" Margin="0 10 0 0" Text="{Binding FailureText}" TextWrapping="Wrap"/>    
        </ScrollViewer>
    </DockPanel>
</UserControl>
