<UserControl
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:global="clr-namespace:"
        x:Class="SuccessView">
    <UserControl.DataContext>
        <global:SuccessViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate x:Key="ItemTemplate" > 
                <Button Command="{Binding Command}" >
                    <Button.Template>
                        <ControlTemplate 
                            TargetType="Button">
                                <Label Padding="0"
                                    Cursor ="Hand"
                                    Foreground="Blue"
                                    Content="{Binding Text}" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="18" FontWeight="Bold">Installation Complete</TextBlock>
        <ItemsControl 
            Grid.Row="1" 
            Focusable="False" 
            ItemTemplate="{StaticResource ItemTemplate}" 
            ItemsSource="{Binding Items}" 
            Margin="0 10"/>
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    FlowDirection="RightToLeft">
            <Button x:Name="Exit" Content="Exit"/>
            <Button x:Name="Home" Content="Back to Main"/>
        </StackPanel>
        <TextBlock Grid.Row="3" Foreground="Gray">Thank you for installing the Particular Service Platform.</TextBlock>

    </Grid>
</UserControl>
